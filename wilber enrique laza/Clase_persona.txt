class Persona:
    # _nombre = "wilber enrique"
    # _edad = 28

    # protegemos el atributo edad para que solo 
    # se pueda modificar a trav�s de m�todos de 
    # la clase.
    @property
    def nombre(self):
        return self.__nombre

    @nombre.setter
    def nombre(self, valor):
        if len(valor) > 0:
            self.__nombre = 
        else:
            raise ValueError("Nombre no v�lido")

    @property
    def edad(self):
        return self.__edad
    
    @edad.setter
    def edad(self, valor):
        if valor >= 0:
            self.__edad = valor
        else:
            raise ValueError("La edad debe ser positiva")

    # constructor
    def __init__(self, nombre, edad):
        self.__nombre = nombre
        self.__edad = edad

    def __str__(self):
        return f"{self.__nombre} ({self.__edad})"
